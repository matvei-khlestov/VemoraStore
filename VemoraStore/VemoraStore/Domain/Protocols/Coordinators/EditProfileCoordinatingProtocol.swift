//
//  EditProfileCoordinatingProtocol.swift
//  VemoraStore
//
//  Created by Matvei Khlestov on 23.09.2025.
//

import Foundation

/// Протокол координатора экрана редактирования профиля пользователя.
///
/// Определяет контракт для навигации при изменении данных профиля
/// (имя, e-mail, номер телефона) и завершения процесса редактирования.
///
/// Используется для управления жизненным циклом экрана и возврата
/// к предыдущему флоу после сохранения изменений.
protocol EditProfileCoordinatingProtocol: Coordinator {
    
    /// Замыкание, вызываемое при завершении редактирования профиля.
    var onFinish: (() -> Void)? { get set }
}
